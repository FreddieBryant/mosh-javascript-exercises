
// for
// while
// do-while

// for-in

// const person = {
//   firstName: 'Freddie',
//   age: 41
// };

// for (let key in person)
//   console.log(key, person[key]);

//  const colors = ['red', 'green', 'blue'];

//  for (let index in colors)
//   console.log(index, colors[index]);

// for (let key in person)
//   console.log(key, person.key);

// Dot Notation
// person.name

// Bracket Notation
// person['name']

// for-of, use it to interate over arrays.
// nextlesson/lecture


//for-in

const person = {
  firstName: 'Freddie',
  age: 41
};

for ( let key in person)
 console.log(key, person[key]);

const colors = ['blue', 'red', 'green']

for (let index in colors)
  console.log(index, colors[index]); 

  